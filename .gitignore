__pycache__/
*.pyc
.cache/

# 이력서 첨부용 PDF 빌드 산출물 (SendUserFile로 전달, 저장소 미포함)
/Argus-README.pdf
/Argus-Deck.pdf

# 대시보드 데이터는 커밋하지 않는다 — 워크플로가 만들어 Pages 아티팩트로 배포한다.
# 커밋하던 시절 시간당 1건씩 쌓여 .git이 100MB가 됐다(cves.json 하나가 18MB).
# 파이프라인 상태는 Supabase pipeline_state 테이블에 있다.
docs/data/*.json

# 로컬 검증용 가상환경
/.venv/
